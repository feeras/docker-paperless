# Docker Compose file for running paperless from the docker container registry.
# This file contains everything paperless needs to run.
# Paperless supports amd64, arm and arm64 hardware.
#
# All compose files of paperless configure paperless in the following way:
#
# - Paperless is (re)started on system boot, if it was running before shutdown.
# - Docker volumes for storing data are managed by Docker.
# - Folders for importing and exporting files are created in the same directory
#   as this file and mounted to the correct folders inside the container.
# - Paperless listens on port 8000.
#
# In addition to that, this Docker Compose file adds the following optional
# configurations:
#
# - Instead of SQLite (default), PostgreSQL is used as the database server.
# - Apache Tika and Gotenberg servers are started with paperless and paperless
#   is configured to use these services. These provide support for consuming
#   Office documents (Word, Excel, PowerPoint and their LibreOffice counter-
#   parts).
#
# To install and update paperless with this file, do the following:
#
# - Copy this file as 'docker-compose.yml' and the files 'docker-compose.env'
#   and '.env' into a folder.
# - Run 'docker compose pull'.
# - Run 'docker compose up -d'.
#
# For more extensive installation and update instructions, refer to the
# documentation.
services:
  broker:
    image: docker.io/library/redis:8
    restart: unless-stopped
    volumes:
      - redisdata:/data
  db:
    image: docker.io/library/postgres:18
    restart: unless-stopped
    volumes:
      - pgdata:/var/lib/postgresql
    environment:
      POSTGRES_DB: paperless
      POSTGRES_USER: paperless
      POSTGRES_PASSWORD: paperless
  webserver:
    image: ghcr.io/paperless-ngx/paperless-ngx:latest
    restart: unless-stopped
    depends_on:
      - db
      - broker
      - gotenberg
      - tika
    ports:
      - "8000:8000"
    volumes:
      - nas_data:/usr/src/paperless/data
      - nas_media:/usr/src/paperless/media
      - nas_consume:/usr/src/paperless/consume
      - nas_export:/usr/src/paperless/export
    env_file: docker-compose.env
    environment:
      PAPERLESS_REDIS: redis://broker:6379
      PAPERLESS_DBHOST: db
      PAPERLESS_TIKA_ENABLED: 1
      PAPERLESS_TIKA_GOTENBERG_ENDPOINT: http://gotenberg:3000
      PAPERLESS_TIKA_ENDPOINT: http://tika:9998
      PAPERLESS_CONSUMER_POLLING: 10
      PAPERLESS_FILENAME_FORMAT: "{{ document_type }}/{{ created_year }}/{{ correspondent }} - {{ title }}"
  gotenberg:
    image: docker.io/gotenberg/gotenberg:8.25
    restart: unless-stopped
    # The gotenberg chromium route is used to convert .eml files. We do not
    # want to allow external content like tracking pixels or even javascript.
    command:
      - "gotenberg"
      - "--chromium-disable-javascript=true"
      - "--chromium-allow-list=file:///tmp/.*"
  tika:
    image: docker.io/apache/tika:3.2.2.0
    restart: unless-stopped
volumes:
  nas_consume:
    driver: local
    driver_opts:
      type: cifs
      device: "//192.168.1.150/PaperlessConsume"
      o: "addr=192.168.1.150,username=${nas_paperless_username},password=${nas_paperless_password},uid=1000,gid=1000,vers=3.0,sec=ntlmssp,file_mode=0777,dir_mode=0777"
  nas_media:
    driver: local
    driver_opts:
      type: cifs
      device: "//192.168.1.150/Shared/Paperless/media"
      o: "addr=192.168.1.150,username=${nas_paperless_username},password=${nas_paperless_password},uid=1000,gid=1000,vers=3.0,sec=ntlmssp,file_mode=0777,dir_mode=0777"
  nas_data:
    driver: local
    driver_opts:
      type: cifs
      device: "//192.168.1.150/Shared/Paperless/data"
      o: "addr=192.168.1.150,username=${nas_paperless_username},password=${nas_paperless_password},uid=1000,gid=1000,vers=3.0,sec=ntlmssp,file_mode=0777,dir_mode=0777"
  nas_export:
    driver: local
    driver_opts:
      type: cifs
      device: "//192.168.1.150/Shared/Paperless/export"
      o: "addr=192.168.1.150,username=${nas_paperless_username},password=${nas_paperless_password},uid=1000,gid=1000,vers=3.0,sec=ntlmssp,file_mode=0777,dir_mode=0777"
  pgdata:
  redisdata: